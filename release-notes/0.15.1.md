## GridSense 0.15.1

- Fixed: column headers scrolled away instead of staying pinned. A style rule added for the column-resize handles was quietly cancelling the sticky header on every column except the row-number one.
